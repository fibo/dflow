{
  "name": "dflow",
  "description": "is a minimal Dataflow programming engine",
  "type": "module",
  "version": "0.37.0",
  "homepage": "https://fibo.github.io/dflow",
  "license": "MIT",
  "main": "./dflow.cjs",
  "types": "dflow.d.ts",
  "exports": {
    ".": {
      "import": "./dflow.js",
      "require": "./dflow.cjs",
      "types": "./dflow.d.ts"
    },
    "./nodes": {
      "import": "./nodes.js"
    }
  },
  "funding": "https://github.com/sponsors/fibo",
  "engines": {
    "node": ">=14",
    "deno": ">=1.10"
  },
  "author": {
    "name": "Gianluca Casati",
    "url": "https://fibo.github.io"
  },
  "scripts": {
    "build": "npm run bundle:dflow:commonjs && npm run bundle:dflow:esmodule && npm run bundle:nodes:esmodule && npm run emit_typescript_declarations",
    "bundle:dflow:commonjs": "deno bundle dflow.ts | esbuild --format=cjs --target=node10 > dflow.cjs",
    "bundle:dflow:esmodule": "deno bundle dflow.ts | esbuild --minify-whitespace --target=safari11 > dflow.js",
    "bundle:nodes:esmodule": "deno bundle nodes/index.ts | esbuild --minify-whitespace --target=safari11 > nodes.js",
    "example:abstract": "deno run examples/abstract.js",
    "example:context": "deno run examples/context.ts",
    "example:execution-report": "deno run examples/execution-report.ts",
    "example:usage": "deno run examples/usage.js",
    "emit_typescript_declarations": "tsc --project ./tsconfig.declarations.json",
    "fmt": "deno task fmt",
    "generate_docs": "npx typedoc --out docs --tsconfig tsconfig.declarations.json dflow.ts",
    "postemit_typescript_declarations": "deno fmt dflow.d.ts",
    "prebuild": "npm test",
    "lint": "deno task lint",
    "postversion": "git push origin v${npm_package_version}; git push origin main; npm publish",
    "test": "deno task test"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/fibo/dflow.git"
  },
  "keywords": [
    "dataflow",
    "visual"
  ],
  "dependencies": {}
}
