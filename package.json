{
  "name": "dflow",
  "description": "is a minimal Dataflow programming engine",
  "version": "0.15.0",
  "homepage": "http://g14n.info/dflow",
  "author": {
    "name": "Gianluca Casati",
    "url": "http://g14n.info"
  },
  "license": "MIT",
  "bin": "src/cli/dflow",
  "main": "index.js",
  "engines": {
    "node": ">=4"
  },
  "browser": "./src/engine/context/client-window.js",
  "scripts": {
    "build": "npm test && npm run browserify && npm run minify && npm run jekyll; npm run git_add-f; git status",
    "bower": "npm run bower_install; npm run git_add-f_bower_components",
    "bower_install": "cd src/editor; bower --silent --config.analytics=false install; cd -",
    "browserify": "for x in dist editor examples test_engine; do npm run browserify_$x; done",
    "browserify_dist": "browserify -r ./src/engine/context/client-window.js:${npm_package_name} -o dist/${npm_package_name}.js",
    "browserify_editor": "browserify src/editor/index.js -t babelify -o src/editor/static/bundle.js",
    "browserify_examples": "echo ***TODO*** browserify -r ./src/examples/renderer/client-side.js:examples-renderer -o gh-pages/js/examples-renderer.js",
    "browserify_test_engine": "NODE_PATH=src browserify test/engine/**/*.js -o gh-pages/test/bundle.js",
    "check-deps": "npm outdated",
    "coverage": "npm run istanbul && npm run coveralls",
    "coveralls": "cat ./coverage/lcov.info | coveralls --verbose",
    "cp_package.json": "cp package.json gh-pages/_data/",
    "decrypt_.coveralls.yml": "keybase decrypt -o .coveralls.yml .coveralls.yml.asc",
    "gh-pages_push": "git subtree --prefix gh-pages push origin gh-pages",
    "gh-pages_pull": "git subtree --prefix gh-pages pull origin gh-pages",
    "git_add-f": "for x in editor_bundle test_bundle; do npm run git_add-f_$x; done",
    "git_add-f_editor_bundle": "git add -f src/editor/static/bundle.js",
    "git_add-f_test_bundle": "git add -f gh-pages/test/bundle.js",
    "homepage": "echo \"---\ntitle: $npm_package_name\n---\" > gh-pages/index.md; cat README.md >> gh-pages/index.md",
    "istanbul": "istanbul cover _mocha -- --recursive",
    "jekyll": "npm run homepage; npm run cp_package.json; cd gh-pages; jekyll build; cd ..",
    "lint": "for x in src test; do npm run lint_$x; done",
    "lint_src": "cd src; standard; cd -",
    "lint_test": "cd test; standard --global describe --global it; cd -",
    "minify": "cd dist; uglifyjs ${npm_package_name}.js --source-map ${npm_package_name}.map --output ${npm_package_name}.min.js --compress --mangle --preamble \"// Software: ${npm_package_name}\n// Homepage: ${npm_package_homepage}\n// License: ${npm_package_license}\"; cd -",
    "postversion": "git push origin v${npm_package_version}; npm publish; npm run _push",
    "postupdate-deps": "npm test && git commit -am 'updated deps' || git checkout -- package.json",
    "pull": "git pull origin master && npm run gh-pages_pull",
    "push": "git push origin master && npm run gh-pages_push",
    "start": "node src/cli/dflow",
    "test": "NODE_PATH=src mocha",
    "update-deps": "npm update --save --dev"
  },
  "pre-commit": [
    "lint",
    "test",
    "check-deps"
  ],
  "dependencies": {
    "babel-plugin-react-transform": "^2.0.2",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0",
    "brfs": "^1.4.3",
    "browserify": "^13.0.0",
    "budo": "^8.3.0",
    "flow-view": "^2.2.0",
    "isomorphic-fetch": "^2.2.1",
    "istanbul": "^0.4.4",
    "livereactload": "^2.2.4",
    "nopt": "^3.0.4",
    "not-defined": "^1.0.0",
    "react": "^15.1.0",
    "react-dom": "^15.1.0",
    "react-redux": "^4.4.5",
    "react-router": "^2.5.0",
    "redux": "^3.5.2",
    "redux-thunk": "^2.1.0",
    "write-file-utf8": "^1.1.0"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/fibo/dflow.git"
  },
  "keywords": [
    "dataflow",
    "visual"
  ],
  "devDependencies": {
    "connect": "^3.4.1",
    "istanbul": "^0.4.3",
    "livereactload": "^2.2.3",
    "mocha": "^2.4.5",
    "mocha-lcov-reporter": "^1.2.0",
    "pre-commit": "^1.1.2",
    "react-proxy": "^1.1.8",
    "should": "^9.0.0",
    "standard": "^7.0.0",
    "supertest": "^1.2.0",
    "uglify-js": "^2.6.2"
  }
}
